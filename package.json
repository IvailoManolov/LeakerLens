{
  "name": "leakerlens",
  "displayName": "LeakerLens",
  "description": "Catch API keys, tokens, and secrets as you type — 100% locally. Nothing leaves your machine.",
  "version": "5.0.2",
  "publisher": "Manolov",
  "license": "SEE LICENSE IN LICENSE",
  "pricing": "Free",
  "repository": {
    "type": "git",
    "url": "https://github.com/IvailoManolov/LeakerLens"
  },
  "engines": {
    "vscode": "^1.75.0"
  },
  "categories": [
    "Linters",
    "Other"
  ],
  "keywords": [
    "secrets",
    "security",
    "api keys",
    "credentials",
    "leak"
  ],
  "galleryBanner": {
    "color": "#0b1220",
    "theme": "dark"
  },
  "icon": "media/icon.png",
  "main": "./dist/extension.js",
  "bin": {
    "leakerlens": "./dist/cli.js"
  },
  "activationEvents": [
    "onStartupFinished"
  ],
  "contributes": {
    "commands": [
      {
        "command": "leakerlens.scanWorkspace",
        "title": "LeakerLens: Scan Workspace for Secrets"
      },
      {
        "command": "leakerlens.focusPanel",
        "title": "LeakerLens: Show Findings Panel"
      },
      {
        "command": "leakerlens.showSecretGraph",
        "title": "LeakerLens: Show Secret Graph"
      },
      {
        "command": "leakerlens.setupAgentGuardrails",
        "title": "LeakerLens: Set up agent guardrails"
      },
      {
        "command": "leakerlens.installGitHook",
        "title": "LeakerLens: Install Pre-commit Guard (this repo)"
      },
      {
        "command": "leakerlens.uninstallGitHook",
        "title": "LeakerLens: Remove Pre-commit Guard (this repo)"
      }
    ],
    "viewsContainers": {
      "activitybar": [
        {
          "id": "leakerlens",
          "title": "LeakerLens",
          "icon": "media/icon.svg"
        }
      ]
    },
    "views": {
      "leakerlens": [
        {
          "id": "leakerlens.panel",
          "name": "Findings",
          "type": "webview"
        }
      ]
    },
    "configuration": {
      "title": "LeakerLens",
      "properties": {
        "leakerlens.enable": {
          "type": "boolean",
          "default": true,
          "description": "Enable real-time secret detection."
        },
        "leakerlens.debounceMs": {
          "type": "number",
          "default": 200,
          "minimum": 50,
          "maximum": 1000,
          "description": "Delay after typing stops before re-scanning a document (ms)."
        },
        "leakerlens.commitBlocking": {
          "type": "boolean",
          "default": false,
          "description": "When the pre-commit guard is installed, block commits that contain secrets. When off, the guard warns only."
        },
        "leakerlens.maxFileSizeKb": {
          "type": "number",
          "default": 2048,
          "description": "Skip real-time scanning of files larger than this (KB) to protect typing latency."
        }
      }
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build:css": "tailwindcss -i ./src/webview/styles.css -o ./dist/webview.css --minify",
    "build:js": "node esbuild.mjs",
    "build": "npm run typecheck && npm run build:js -- --production && npm run build:css",
    "watch": "node esbuild.mjs --watch",
    "test": "vitest run --coverage",
    "test:watch": "vitest",
    "pretest:integration": "node -e \"require('fs').rmSync('out-test',{recursive:true,force:true})\" && tsc -p ./tsconfig.test.json && node -e \"const fs=require('fs');fs.cpSync('test/integration/vscode-shim.js','out-test/test/integration/vscode-shim.js');fs.cpSync('test/integration/vscode-shim.cmd','out-test/test/integration/vscode-shim.cmd')\"",
    "test:integration": "node ./out-test/test/integration/runTest.js",
    "package": "npm run build && vsce package --no-dependencies",
    "vscode:prepublish": "npm run build"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.6",
    "@types/node": "^20.11.0",
    "@types/vscode": "^1.75.0",
    "@vitest/coverage-v8": "^1.6.0",
    "@vscode/test-electron": "^2.5.2",
    "@vscode/vsce": "^3.9.2",
    "esbuild": "^0.20.2",
    "glob": "^10.3.10",
    "mocha": "^10.4.0",
    "tailwindcss": "^3.4.3",
    "typescript": "^5.4.5",
    "vitest": "^1.6.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "ignore": "^5.3.2",
    "zod": "^3.25.76"
  }
}
